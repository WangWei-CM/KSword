1.内核部分UI重写。分tab展示这些功能，第一级tab是对象命名空间、内核信息、系统调用、内核狗子、驱动诊断、回调，第二级是剩下的细分项目。现有的项目数量上是没有任何问题的。每一个项目的执行结果要和KswordARK本体一样明确展示，以表格的方式。已完成
2.监控列表最左侧必须渲染PID对应进程的图标。已完成
3.双击监控项能打开这一项详细信息。直接抄KswordARK。已完成
4.默认的ETW筛选器可能少了一点。系统自带的都带上。已完成
5.硬件部分原来右键过后是可以卸载等操作的。找KswordARK的代码。已完成
6.窗口列表也要渲染进程图标和名字，合并到PID列。已完成
7.窗口排序方式在前面添加下拉菜单选项：按堆叠顺序或按进程顺序已完成
8.启动项尽力渲染对应的进程图标。已完成
9.把KswordARKLight接入真正的驱动，复用share，保证ARK工具最终可用，进行校对，尽量减少UI bug，保证所有驱动调用方式都是正确的，满足IOCTL，参考KswordARK项目进行核对验证。着手去修改代码，不要一直东看西看，确定了就直接去落地。已完成
10.进程列表不要使用制表符，自己绘制缩进的网格线，进程图标也要一起缩进。“应用”“后台进程”“系统进程”前面不要有应用程序图标。已完成
11.文件dock里面，所有文件和目录都要在前面渲染图标。右键菜单中没有实现的请删去，不要放一个灰色的在那里已完成
12.驱动页面右键菜单应该保留弹出详细信息和右键调用卸载的IOCTL，具体请参考原KswordARK已完成
13.进程部分图形界面你修出bug了：鼠标悬停经过的行全部变白了，等到下一次刷新才会出现字。已完成
14.进程部分还有问题：应该匹配上一次进程列表，新增的或删除的分别以绿色和灰色高亮；不做全表重绘。已完成
