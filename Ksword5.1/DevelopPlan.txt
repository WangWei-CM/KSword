任务：
查看所有全局源代码。当我们结束进程，或者任何类似操作的时候，不管成功或者失败只应该输出日志，不要弹出msgbox，很打扰用户，因为要多一次点击。（已完成）
然后，在开发规范中有如下新增的句子：
最重要的一点：kLogEvent的用途是追踪调用链。因此，在连续的过程中只应该有一个kLogEvent变量，让整个操作中的kLogEvent都是一样的，而不要随便到处临时创建临时的kLogEvent。
之前的开发过程可能没有遵守此规范。请你修改文件中所有的日志部分，看看有没有不遵守规范的离散日志，如果有，整合。（已完成）
新增任务：驱动模块增强，包含驱动服务枚举、服务注册/更新、驱动挂载/卸载、服务删除、已加载模块枚举、调试输出读取（DBWIN）。（已完成）
