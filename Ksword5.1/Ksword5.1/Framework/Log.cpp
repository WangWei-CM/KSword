#include "../Framework.h"

// The logging implementation was moved to ksword/log/log.cpp so reusable R3
// tools can include Ksword.h without depending on Framework internals.  This
// translation unit is intentionally kept as a compatibility placeholder because
// the Visual Studio project already references Framework\Log.cpp.
