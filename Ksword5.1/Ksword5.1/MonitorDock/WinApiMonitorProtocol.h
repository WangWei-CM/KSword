#pragma once

// ============================================================
// WinApiMonitorProtocol.h
// 作用：
// 1) 保留 WinAPIDock 侧既有包含路径，避免主程序其他代码继续引用时失效；
// 2) 实际协议定义收口到仓库根目录 shared，确保 UI 与 Agent 使用同一份协议；
// 3) 本文件只做轻量转发，不再维护独立协议副本。
// ============================================================

#include "../../../shared/WinApiMonitorProtocol.h"
