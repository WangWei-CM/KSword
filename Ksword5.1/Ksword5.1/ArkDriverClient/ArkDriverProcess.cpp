#include "ArkDriverClient.h"

// Phase -1 currently keeps process operations in ArkDriverClient.cpp so related
// parsing helpers stay local. This translation unit reserves a stable project
// file for later process-specific client expansion without touching Dock files.
namespace ksword::ark
{
}
