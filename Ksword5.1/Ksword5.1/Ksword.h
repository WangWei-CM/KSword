#pragma once

// ============================================================
// Ksword.h
// 作用：
// 1) 作为根目录级别的 Win32 工具入口头文件；
// 2) 对外统一暴露 ks 命名空间下的所有能力；
// 3) 满足“脱离 KswordARK 也可复用”的封装目标。
// ============================================================

// 说明：
// - 具体实现代码放在根目录下的 ksword/ 递归文件夹中；
// - 业务侧只需要 include "Ksword.h" 即可。
#include "ksword/ksword.h"

