#pragma once

/*
 * Ksword wrapper for the System Informer autogenerated DynData blob.
 *
 * This file provides only the kernel/DDK type surface required by kphdyn.c and
 * kphdyn.h. It intentionally does not import KPH communication, object, crypto,
 * trace, or session-token infrastructure.
 */

#include <ntddk.h>

typedef UCHAR BYTE;

#ifndef ARRAYSIZE
#define ARRAYSIZE(ArrayValue) (sizeof(ArrayValue) / sizeof((ArrayValue)[0]))
#endif

#include "kphdyn.h"
