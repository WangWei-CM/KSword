Useful commands on Windows PowerShell: `git status --short --branch`, `git log --oneline --decorate -n 20`, `git diff --check`, `git diff --name-status A..B`, `git show --stat <commit>`, `rg <pattern> <path>`, `gh pr list --repo Felix3322/KSword --state closed`, `gh pr view <n> --repo Felix3322/KSword --json ...`. Build is Visual Studio/MSBuild via `.sln`/`.vcxproj`; use CLion build tool when available for validation.